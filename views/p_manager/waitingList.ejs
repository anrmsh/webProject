<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <title>Расписание — Панель менеджера</title>
    <link rel="stylesheet" href="/css/p_manager/manager.css" />
    <!-- <link rel="stylesheet" href="/css/p_manager/schedule.css" /> -->
    <link rel="stylesheet" href="/css/p_manager/waitingList.css" />
  </head>
  <body>
    <header>
      <a href="/" class="logo">BanquetBook<span>.</span></a>
      <nav class="navbar">
        <a href="/manager/waiting-list">Лист ожидания</a>
        <a href="/manager/schedule" class="active">Расписание</a>
        <a href="/manager/report">Отчёты</a>
      </nav>
    </header>

    <main class="schedule-container">
      <h2>Расписание залов</h2>
      <div class="controls">
        <label for="hallSelect">Зал:</label>
        <select id="hallSelect">
          <% if (halls && halls.length) { %> <% halls.forEach(h => { %>
          <option value="<%= h.hall_id %>"><%= h.hall_name %></option>
          <% }) %> <% } else { %>
          <option value="">Нет назначенных залов</option>
          <% } %>
        </select>

        <label for="dateInput">Дата:</label>
        <input
          type="date"
          id="dateInput"
          value="<%= new Date().toISOString().slice(0,10) %>"
        />

        <button id="refreshBtn" class="btn-small">Обновить</button>
      </div>

      <div class="content-grid">
        <div id="timeline" class="timeline">
          <div class="empty">
            Выберите зал и дату, чтобы увидеть мероприятия
          </div>
        </div>

        <div id="waitingList" class="waiting-list">
          <h3>Лист ожидания</h3>
          <div class="list-content empty">Нет заявок</div>
        </div>
      </div>
    </main>

    <!-- Модальное окно для деталей -->
    <div id="modal" class="modal-overlay" style="display: none">
      <div class="modal-box">
        <div id="modalIcon" class="modal-icon"></div>
        <h2 id="modalTitle"></h2>
        <div id="modalBody"></div>
        <button id="closeModal">Закрыть</button>
      </div>
    </div>

    <script src="/script/p_manager/waitingList.js"></script>
  </body>
</html>
